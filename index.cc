#include <iostream>

int main() {
    // For loop to print numbers from 1 to 10
    for (int i = 1; i <= 10; ++i) {
        std::cout << i << " "; // Print the current value of i followed by a space
    }
    std::cout << std::endl; // Print a newline after the loop completes

    return 0;
}
